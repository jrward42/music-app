<h1> Welcome to MusicApp </h1>

<h2> All bands: </h2>

<% @bands = Band.all %>
<ul>
<% @bands.each do |band| %>
<li> <%= link_to band.name, band_url(band) %> </li>
  <% end %>
<ul>
<%= link_to "Create New Band", new_band_url%>
